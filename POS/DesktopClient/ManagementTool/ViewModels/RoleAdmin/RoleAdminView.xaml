<Window x:Class="ManagementTool.ViewModels.RoleAdmin.RoleAdminView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit" 
        mc:Ignorable="d"
        xmlns:viewModels="clr-namespace:ManagementTool.ViewModels.RoleAdmin"
        d:DataContext="{d:DesignInstance viewModels:RoleAdminVm}"
        Title="RoleAdminView" Height="800" Width="800">
    <Grid>
        <StackPanel>
        <TextBlock Text="This window is only accessible for administrators..."/>
        
        <Label FontSize="20">Add Role</Label>
        
            <StackPanel Orientation="Horizontal">
        <Label>Role Name</Label>
        <TextBox Text="{Binding RoleName, 
                                UpdateSourceTrigger=PropertyChanged, 
                                ValidatesOnDataErrors=True}" 
                 MinWidth="175">
        </TextBox>
        <Button Content="Add Role" Command="{Binding AddRoleCommand}"
              HorizontalAlignment="Center" MaxHeight="25" MaxWidth="65"/>
        </StackPanel>
        </StackPanel>
    </Grid>
</Window>
