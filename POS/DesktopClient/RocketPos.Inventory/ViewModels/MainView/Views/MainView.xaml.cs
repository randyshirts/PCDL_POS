namespace Inventory.ViewModels.MainView.Views
{
	public partial class MainView
	{
		public MainView()
		{
			// Required to initialize variables
			InitializeComponent();
            //Closing += (s, e) => ViewModelLocator.Cleanup();
		}
	}
}